EditAccess to ProjectProvider:
- ProjectLoader
- ReadOnlyProjectProvider

Domain?: 
- ReadOnlyInterfaces

State:
- State:
	- Timestamp of creation
	- ParentState (ReadOnlyDataContext)
	- MainState (CoreDataContext)
	- CloneState (ClonedDataContext)

DataContextCloning
DataContextComparing


CC commands:
- project info:
	- ud
- evaluate dependencies:
	- find unused:
		- foreach Dependency any UDs?
	- find already referenced:
		- start toplevel internal:
			group by layer
			foreach layer, project, dependency:
				if dependency in other dependency
				=> "flag"
		-speed up with layers? 